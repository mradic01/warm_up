import React from 'react';

import classes from './logo.module.css';

const logo = () => (
    <section className={classes.Logo}>
        <span className={classes.LogoText}>Villa Oliva Verde</span>
        <span className={classes.Sub}>ISTRA-CROATIA</span>
    </section>
);

export default logo;